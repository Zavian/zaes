# zaes.dev

Personal homepage and technical portfolio of Emanuele Sbabo — web developer, technical QA analyst, and systems builder.

## Run locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the dev server:
   `npm run dev`

## Build

`npm run build` outputs the static site to `dist/`.
